{
  "hash": "696a64856bd1c4b94ec11e8995640fed",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: Homework 1 - One Sample Inference\n\n# Bibliography Details\nbibliography: hw1References.bib  # Name of bib file in the same folder \nbibliographystyle: apa            # Bibliography style\n\nexecute:               # Default settings for code blocks\n  warning: false       # Do not print warnings\n  message: false       # Do not print messages\n  size: \"scriptsize\"   # Set code size to script size\n\n# Below will do html by default. To render the entire project\n# Use the following code:\n# quarto::quarto_render(\"Lab10WriteUp.qmd\")\nformat:\n  pdf:        # Specify .pdf formatting options \n    documentclass: article     # Document Class\n    number-sections: true      # Number Sections\n    reference-section-title: \"References\" # Title for reference section\n    code-line-numbers: true    # Line numbers printed in code\n    cap-location: top          # Captions at the top of tables \n    geometry:\n      - top=0.75in\n      - left=0.75in\n      - bottom=0.75in\n      - right=0.75in\n  html:       # Specify HTML formatting options \n    number-sections: true      # Number Sections\n    code-fold: true            # Button to show code\n    code-line-numbers: true    # Line numbers printed in code\n    cap-location: top          # Captions at the top of tables and figures\nformat-links: [pdf]\n---\n\n# Question 1\n\nWe are presented with the results of a study conducted by Dror et al. on the capacity of dogs to passively learn vocabulary when immersed in social situations,\nsimilar to the established language learning capacity of toddlers. This was evaluated by whether the dogs were able to correctly return both familiar toys and the newly introduced \ntoys that the dogs overheard reference to throughout the study period.\n\nWe are presented with two results: (1) the proportion of requests for new toys completed, for each dog and (2) the paired difference between this proportion and the proportion of requests\ncompleted for the familiar toys (familiar - new).\n\n($1$) = $\\{1.0000, 1.0000, 0.7500, 1.0000, 0.4167, 0.8333, 0.7500, 0.8333, 0.8333, 1.0000\\}$\n\n($2$) = $\\{−0.0833, 0.0000, −0.0833, −0.0833, 0.5833, 0.0833, −0.2500, 0.1667, 0.0000, −0.4167\\}$\n\n\n\n::: {.cell}\n\n:::\n\n\n\n::: {.cell}\n\n:::\n\n\n## Part A: Choice of Tests\nThe authors analyzed both datasets using the Wilcox signed-rank test. For dataset (1), they were considering whether the median dog was better than chance, which is intepreted as a proportion bigger than $.5$. Although it's not stated in the assignment, we can assume that although they were interested in only one tail, a two-tailed Wilcox test was most likely used. Although we aren't told whether they used the exact or approximate test, we don't have a sufficiently large sample size to be comfortable with the asymptotic behavior kicking in, suggesting they should not have used the approximate. Regarding the choice of the median as the statistic of interest, it could be explained by concern about left skew, which is clearly visible in @fig-q1-distributions as due to a single outlier dog who might not be that gifted after all. Unfortunately, the Wilcox signed-rank test assumes an underlying symmetric distribution, and thus is not appropriate for this data. Considering the alternative tests available, to use a proportion based test, we could manipulate the data by considering the binary event for each observation as $(p > .5)$. This is to say that our indicator variable for each dog asks: \"did this dog perform better than chance?\" Our sample proportion is $.9$, since $9$ of the $10$ dogs had a response rate greater than $.5$. Although this significantly simplifies the richness of the data, it is still applicable for the research question. Since none of the other tests we've discussed are appropriate: Wilcox requires symmetry, t requires normality, z requires bernoulli, while t and z can rely on sample size conditions; the exact binomial is our best options out of the discussed parametric tests.\n\nFor dataset (2), we are under the same constraints regarding the sample size and lack of normality, though the data is now fairly symmetrical. Unfortunately, due to the presence of ties in the dataset, we are unable to use the exact Wilcox. Since we still can't rely on approximate behavior, we could again default to the exact binomial by constructing an indicator variable. Constructing an indicator variable for this data is more nuanced, since we are interested in the distance from 0, rather than just the direction. As such, I prefer to proceed by performing bootstrap one-sample mean inference directly on the difference data.\n\n## Part B: Analysis\n\nGiven the collected response proportion data, we are interested in whether the population of gifted word learner dogs performs better than chance on tasks requiring passive vocabulary learning and whether their performance significantly differs on toys that they have directly learned to recognize. Before describing the inferential techniques we employ, we present visual and numerical in @fig-q1-distributions and @tbl-q1-summaries.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmake.plots = function(x, title, xlab, binwidth){\n  ggdat = tibble(value = x)\n\n  hist.plot = ggplot(ggdat, aes(x = value)) +\n    geom_histogram(binwidth = binwidth, boundary = 0,\n                   color = \"black\", fill = \"lightgrey\") +\n    geom_rug(alpha = 0.6) +\n    labs(x = xlab, y = \"Count\", title = title) +\n    theme_bw()\n\n  box.plot = ggplot(ggdat, aes(x = value, y = \"\")) +\n    geom_boxplot(fill = \"lightgrey\") +\n    labs(x = xlab, y = \"\") +\n    theme_bw()\n\n  hist.plot / box.plot + plot_layout(heights = c(2, 1))\n}\n\np1 = make.plots(new.props, \"(1) New toy proportions\",\n                \"Proportion of new toy requests \", binwidth = 0.10)\np2 = make.plots(paired.diffs, \"(2) Paired differences\",\n                \"Familiar - new proportion\", binwidth = 0.10)\n\n(p1 | p2)\n```\n\n::: {.cell-output-display}\n![Histograms and boxplots of the new-toy proportions (1) and the paired differences (2)](hw1_files/figure-pdf/fig-q1-distributions-1.pdf){#fig-q1-distributions fig-pos='H'}\n:::\n:::\n\n\n\n::: {#tbl-q1-summaries .cell tbl-cap='Numerical summaries of the new-toy proportions (1) and the paired differences (2)'}\n\n```{.r .cell-code}\ndat.q1 |>\n  group_by(dataset) |>\n  summarize(\n    n        = n(),\n    mean     = mean(value),\n    median   = median(value),\n    sd       = sd(value),\n    skewness = e1071::skewness(value, type = 2),\n    min      = min(value),\n    max      = max(value)\n  ) |>\n  knitr::kable(digits = 4)\n```\n\n::: {.cell-output-display}\n\n\n|dataset                                |  n|    mean|  median|     sd| skewness|     min|    max|\n|:--------------------------------------|--:|-------:|-------:|------:|--------:|-------:|------:|\n|(1) New-toy proportion                 | 10|  0.8417|  0.8333| 0.1819|  -1.4394|  0.4167| 1.0000|\n|(2) Paired difference (familiar - new) | 10| -0.0083| -0.0416| 0.2648|   0.9701| -0.4167| 0.5833|\n\n\n:::\n:::\n\n\n### New Toy Response\n\nDue to the small sample size, skew, and presence of ties in the new toy proportions dataset, we perform our analysis using the exact binomial test on the proportion of dogs that perform better than chance. We construct the indicator $(p > .5)$ for each member of our sample. We then calculate the proportion of dogs that perform better than chance. Our null hypothesis, $H_0$ is that $p=.5$, and our alternative hypothesis $H_a$ is that $p \\ne .5$. Our results from a two-sided exact binomial test at a confidence level of $95\\%$ include a p-value of $.02$ and a $95\\%$ confidence interval of $(.555,.997)$. Taken together, our test results suggest that the population proportion of dogs that perform better than chance is significantly different from $.5$, and we are $95\\%$ confident that the true population value falls in the range $(.555,.997)$\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(41)\nsuccess.count = sum(ifelse(new.props > .5, 1, 0))\ntest = binom.test(\n  x = success.count,\n  n = length(new.props),\n  p = .5,\n  alternative = \"two.sided\",\n  conf.level = .95\n)\n(test$p.value)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.02148438\n```\n\n\n:::\n\n```{.r .cell-code}\n(as.numeric(test$conf.int))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.5549839 0.9974714\n```\n\n\n:::\n:::\n\n\n### Paired Differences \nFor our analysis of the paired differences, we are interested in whether there is difference between the dog's response rate for familiar and new toys. Our null hypothesis $H_0$ is that $\\mu = 0$ and the alternative $H_a$ is that $\\mu \\ne 0$. As mentioned previously we are unable to use the exact Wilcox signed-rank test for our paired dataset due to the presence of ties. As such, we proceed with a bootstrap analysis. While we could apply a bootstrap test that requires shifting the data, to avoid any complications that may arise when a shift in the mean affects the variance, we instead directly produce a bootstrap $95\\%$ confidence interval. We report the interval as $(-.150,1.58)$. As such, we do not find convincing evidence of the alternative hypothesis, so our results suggest that the population difference is not significantly different from 0.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(41) \nB = 10000\nn = length(paired.diffs)\n\nboot.dist = numeric(B)   # storage for the resampled means\n\nfor(i in 1:B){\n  resample = sample(paired.diffs, size = n, replace = TRUE)\n  boot.dist[i] = mean(resample)\n}\n\nboot.ci = quantile(boot.dist, probs = c(0.025, 0.975))\n\n(as.numeric(boot.ci))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] -0.1500102  0.1583300\n```\n\n\n:::\n:::\n\n\n\n# Question 2\n\nNow, the researchers are consider the proportion of dogs who were able to retrieve the new toys. They report the proportion as 1, meaning that all 10 dogs were able to retrieve the toy.\n\n## Part A\n\nThe binomial distribution is suited for a sum of independent identical bernoulli variables. Whether or not an individual dog retrieved the correct toy is clearly a binary event. Although the individual events are not independent, since the dogs appear in multiple trials, the within dog differences are expected to be independent across dogs. It's also reasonable to asume that the sample is representative of the population, though this is rarely verifiable. Since the test is exact, we don't require any assumptions regarding sample size for asymptotic behavior to be relevant. Using discrete distributions for statistical tests is made more difficult by the discontinuity of the CDF making it impossible to generally specify an exact confidence level.\n\n## Part B\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsuccess = 10\ntrials = 10\nsuccess.prob = .25\n(prob = dbinom(x=success,size=trials,prob=success.prob))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 9.536743e-07\n```\n\n\n:::\n:::\n\n\nThere is a roughly $.1\\%$ probability of all 10 dogs getting both toys right on the first try assuming that the probability of any individual dog accomplishing the task in $.5$. As considering the random variable such that a dog gets both toys right, the overall probability could be estimated as $.25$, though this assumes independence, which doesn't hold.\n\n\n## Part C\n\nUsing a normal approximation:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmu = trials * success.prob\nsd = sqrt(mu*(1-success.prob))\nepsilon = .5\n(prob = pnorm(1 + epsilon ,mu,sd) - pnorm(1 - epsilon,mu,sd))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.1605409\n```\n\n\n:::\n:::\n\n\nThe results of the normal approximation heavily depend on the choice of epsilon, though they generally agree with the binomial in that the observation is highly unlikely.\n\n## Part D\n\nYes, even with somewhat obviously positive results in the sample inference is still important. Principally, without inference conclusions can only be drawn for the sample, rather than the population. Scientists generally aren't interested in making claims about a particular collection of clever dogs, they are interested in making claims about an entire population of clever dogs. Inference is necessary for the process because it allows us to evaluate the strength of the evidence of the sample for making judgements about the entire population. With this information, we can make qualified claims about the population with pre-determined levels of confidence.\n\nGiven that, we perform a two-sided exact binomial test at a $95\\%$ confidence level and report the results below\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(41)\ntest = binom.test(\n  x = 10,\n  n = 10,\n  p = success.prob,\n  alternative = \"two.sided\"\n)\n\n(test$p.value)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 9.536743e-07\n```\n\n\n:::\n\n```{.r .cell-code}\n(as.numeric(test$conf.int))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.6915029 1.0000000\n```\n\n\n:::\n:::\n\n\nGiven the very small p-value and a confidence interval that excludes $.5$, our test suggests that the population proportion significantly differs from $.5$. \n\n# AI Use Appendix:\n\n* I described psuedo code for the bootstrap test and fed a chat model previous code implementing bootstrap tests to confirm my understanding of the implementation details, the model outputted code which I significantly simplified.\n\n* I forgot how to set the transparency of histogram figures, so I fed in previous code from 240 creating histograms and prompted the chat model to modify the code with a transparent effect. This is where geom_rug came from. \n\n\n\n\n\n\n",
    "supporting": [
      "hw1_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": null,
    "postProcess": false
  }
}